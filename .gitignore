# ===== 依存関係（将来 npm 系ツールを導入した場合に備える） =====
node_modules/

# ===== ビルド成果物 =====
dist/
build/

# ===== 環境変数・機密（git追跡を必ず止める＝必須）=====
.env
.env.*
# 共有用テンプレだけは追跡する（!行にインラインコメントを付けると無効化されるため、コメントは必ず別行に書く）
!.env.example

# ===== ログ・一時ファイル =====
*.log
